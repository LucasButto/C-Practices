#include <stdio.h>
#include <stdlib.h>

void main()
{
    /* 3. Imprimir los números del 10 al 1 uno abajo del otro. */
    int i;

    for(i=10; i>=1; i--)
    {
        printf("%d \n",i);
    }
    system("pause");
}